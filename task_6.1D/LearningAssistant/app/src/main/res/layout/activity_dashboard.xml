<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Header Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="4dp">

            <TextView
                android:id="@+id/tvGreeting"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Hello,\nStudent"
                android:textColor="@color/white"
                android:textSize="28sp"
                android:textStyle="bold" />

            <Button
                android:id="@+id/btnLogout"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="Logout"
                android:backgroundTint="#33FFFFFF"
                android:textColor="@color/white"
                android:textSize="13sp"
                android:contentDescription="Logout button" />
        </LinearLayout>

        <!-- Tasks due badge -->
        <TextView
            android:id="@+id/tvTasksDue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="You have 4 tasks due"
            android:textColor="@color/colorGold"
            android:textSize="14sp"
            android:drawableStart="@android:drawable/presence_online"
            android:drawablePadding="4dp"
            android:layout_marginBottom="20dp" />

        <!-- Task cards label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Generated by AI"
            android:textColor="@color/colorSubtext"
            android:textSize="12sp"
            android:drawableStart="@android:drawable/star_on"
            android:drawablePadding="4dp"
            android:layout_marginBottom="8dp" />

        <!-- Task list -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvTasks"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:contentDescription="Learning tasks list" />

        <!-- Study Plan Section -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AI Study Plan"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="8dp" />

        <Button
            android:id="@+id/btnStudyPlan"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="Generate 7-Day Study Plan"
            android:backgroundTint="@color/colorAccent"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:contentDescription="Generate 7-day personalized study plan button" />

        <ProgressBar
            android:id="@+id/progressStudyPlan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="12dp"
            android:visibility="gone" />

        <!-- Prompt display -->
        <TextView
            android:id="@+id/tvStudyPlanPrompt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_ai_response"
            android:padding="12dp"
            android:textColor="@color/colorSubtext"
            android:textSize="13sp"
            android:layout_marginTop="12dp"
            android:visibility="gone" />

        <!-- AI Response -->
        <TextView
            android:id="@+id/tvStudyPlanResponse"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_card"
            android:padding="16dp"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:lineSpacingExtra="4dp"
            android:layout_marginTop="8dp"
            android:visibility="gone" />

        <!-- Error -->
        <TextView
            android:id="@+id/tvStudyPlanError"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/colorError"
            android:textSize="14sp"
            android:layout_marginTop="8dp"
            android:visibility="gone" />

    </LinearLayout>
</ScrollView>
